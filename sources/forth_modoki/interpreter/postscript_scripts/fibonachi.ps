/fibo { 
  dup 2 le
  { 1 exch pop }
  { 
    1 1  %[x 1 1]
    3 2 roll 3 sub %[1 1 x-3]
    { dup 0 neq }
    { 
      3 1 roll %[idx i-1 i]
      dup 2 index add %[idx i-1 i i+i-1]
      3 2 roll pop %[idx i i+i-1]
      3 2 roll 1 sub %[i i+i-1 idx-1]
    }
    while
    pop
    add
  }
  ifelse
} def
20 fibo
%EXPECTED: 6765